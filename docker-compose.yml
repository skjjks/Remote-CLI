version: '3.8'

services:
  remote-cli:
    build: .
    container_name: remote-cli
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
